name: Sync to Serv00
on:
  push:
    branches:
      - main
    paths:
      - 'users.json'
      - 'config.json'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📦 Prepare files
        run: |
          mkdir -p ./build
          # 复制需要同步的文件
          cp -f users.json config.json ./build/ 2>/dev/null || echo "Files copied or not found"

      - name: 🚀 Sync files to Serv00 (SFTP)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/404-vpn/
          local-dir: ./build/
          protocol: sftp  # 重要：使用 SFTP 协议
          port: 22        # SFTP 默认端口
          # 可选：启用调试模式查看详细信息
          # log-level: verbose
